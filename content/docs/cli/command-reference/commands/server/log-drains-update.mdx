---
title: "Log drains update"
description: "Update log drain settings via JSON"
---

# Log drains update

Update log drain settings via JSON

## Usage

```sh
coolify server log-drains update <server_uuid>
```

## Options

| Option | Type | Required | Default | Description |
| :--- | :--- | :---: | :--- | :--- |
| `--json` | `string` | No | — | JSON fields to patch |

## Get current help

```sh
coolify server log-drains update --help
```

The installed CLI help reflects the exact options available in your CLI version.
