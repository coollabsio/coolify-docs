---
title: "Clone"
description: "Clone an application to a destination"
---

# Clone

Clone an application to a destination

## Usage

```sh
coolify app clone <uuid>
```

## Options

| Option | Type | Required | Default | Description |
| :--- | :--- | :---: | :--- | :--- |
| `--clone-volumes` | `boolean` | No | `false` | Clone volume data |
| `--destination` | `string` | No | — | Destination UUID |
| `--name` | `string` | No | — | Optional new name |

## Get current help

```sh
coolify app clone --help
```

The installed CLI help reflects the exact options available in your CLI version.
