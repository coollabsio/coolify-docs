---
title: "Use"
description: "Switch to a different context (set as default)"
---

# Use

Switch to a different context (set as default)

## Usage

```sh
coolify context use <context_name>
```

## Get current help

```sh
coolify context use --help
```

The installed CLI help reflects the exact options available in your CLI version.
