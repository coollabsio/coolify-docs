---
title: Secret Manager Credentials
description: Create credentials for Doppler, Infisical, or HashiCorp Vault and add them to Coolify.
---

# Secret manager credentials

Coolify supports Doppler, Infisical, and HashiCorp Vault. Add credentials under **Keys & Tokens > Integration Tokens**, then choose the token on a resource's **Environment Variables** page.

## Doppler

Use either credential type:

- **Service token:** In Doppler, open the project and config, select **Access**, and create a service token. Its scope fixes the project and config.
- **Service account token:** Create a service account, grant it access to the required project/config, and generate a token. You select the project and config in Coolify.

Copy the token into Coolify. See [Doppler service tokens](https://docs.doppler.com/docs/service-tokens?utm_source=coolify.io) and [service accounts](https://docs.doppler.com/docs/service-accounts?utm_source=coolify.io).

## Infisical

1. Create a **Machine Identity** in Infisical.
2. Add **Universal Auth** to the identity and generate a client secret.
3. Grant the identity access to the project and environment containing your secrets.
4. In Coolify, enter the Infisical URL, client ID, and client secret.

For Infisical Cloud, use `https://app.infisical.com`. Self-hosted installations should use their own base URL. See [Infisical Universal Auth](https://infisical.com/docs/documentation/platform/identities/universal-auth?utm_source=coolify.io).

## HashiCorp Vault

Create a Vault policy that permits `read` and `list` on the required KV v2 secret path, then issue a token with that policy. In Coolify, enter:

- the Vault base URL
- the token
- the namespace, only for Vault Enterprise or HCP Vault

When linking a resource, select the KV v2 mount and secret path. Use the narrowest policy possible. See [Vault tokens](https://developer.hashicorp.com/vault/docs/concepts/tokens?utm_source=coolify.io) and [policies](https://developer.hashicorp.com/vault/docs/concepts/policies?utm_source=coolify.io).

<Callout type="warning" title="Protect provider credentials">

Provider credentials can expose multiple secrets. Use least-privilege access, rotate credentials regularly, and do not paste them into environment variables.

</Callout>

Next, [use the secret manager with a resource](/integrations/security/using-secret-managers).
